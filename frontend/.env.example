# Base URL of the Spring Boot backend.
# Local development default matches the backend's CORS whitelist (see
# SecurityConfig.java) — keep the dev server on port 5173 or update the
# backend's allowed origins too.
VITE_API_BASE_URL=http://localhost:8080
